spring:
  application:
    name: order-service
  main:
    lazy-initialization: true

server:
  port: 4080


## Make sure actuator endpoints are secured
management:
  endpoints:
    web:
      exposure:
        include: health,info
